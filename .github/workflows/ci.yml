name: CI
on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.26'
          cache: true
      - name: Build internal packages
        run: go build ./internal/...
      - name: Vet
        run: go vet ./internal/...
      - name: Unit tests
        run: go test -count=1 -short ./internal/...
      - name: Safety-critical + whitelist tests
        run: go test -count=1 -timeout=120s -run 'TestCritical|TestChaos|TestWhitelist' ./internal/modes/
      - name: Fuzz seed corpus validation
        run: go test -count=1 -timeout=30s -run 'TestSeed_' ./internal/modes/
